{
  "name": "fcm-trigger",
  "version": "1.0.0",
  "description": "",
  "main": "index.mjs",
  "scripts": {
    "test": "jest",
    "build": "rm -rf dist && tsc && mkdir -p dist/layer/nodejs && cp -r node_modules dist/layer/nodejs"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.525.0",
    "@aws-sdk/client-scheduler": "^3.525.0",
    "@types/aws-lambda": "^8.10.134",
    "source-map-support": "^0.5.21",
    "trash-common": "github:quotto/throwtrash-common-module#v3.0"
  },
  "type": "module",
  "author": "",
  "license": "Apache-2.0",
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/source-map-support": "^0.5.10",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2"
  }
}
