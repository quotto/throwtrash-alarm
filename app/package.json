{
  "name": "app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm install --ws --install-links && npm run build -ws"
  },
  "author": "",
  "license": "Apache-2.0",
  "workspaces": [
    "packages/api/create",
    "packages/api/delete",
    "packages/api/update",
    "packages/trigger",
    "packages/core"
  ],
  "devDependencies": {
    "@types/aws-lambda": "^8.10.136"
  }
}
