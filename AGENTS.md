# AGENTS.md

このドキュメントは、`throwtrash-alarm` プロジェクトで作業するエージェント向けの行動指針を定義する。

## 1. 目的と概要
- 目的: ゴミ出しアラーム機能のアプリケーション (`app/`) とインフラ (`infra/`) を、安全かつ一貫した手順で改善する。
- 対象技術:
  - `app/`: TypeScript (`.mts`) / npm workspaces / AWS Lambda 関連実装
  - `infra/`: Terraform
- 変更方針: 要件に直接関係する最小変更を行い、テストと検証結果を必ず残す。

## 2. 共通ルール
- 応答は日本語で行う。
- 実装前に関連ファイルを確認し、前提が不明な場合はユーザーに確認する。
- 既存の設計・命名規則・ディレクトリ構成を優先し、不要なリファクタを行わない。
- 既存変更を勝手に巻き戻さない。自分が触っていない差分は保持する。
- 破壊的コマンド（`git reset --hard` など）はユーザー明示指示なしで実行しない。
- 生成・修正したコードには、必要最小限のテストを追加または更新する。

## 3. 作業フロー

### 3.1 調査
- 要求を分解し、影響範囲（`app/packages/*`、`infra/*`、`.github/workflows/*` など）を特定する。
- 仕様・挙動の確認が必要な場合は、既存テストと関連実装を先に読む。
- 外部仕様に依存する変更は、公式ドキュメントを優先して確認する。

### 3.2 計画
- 中規模以上の変更では、実装前にタスクを分割して順序を決める。
- 必要に応じて `work/tasks/` に計画を記録し、スコープ外作業を明確に除外する。
- 計画段階では原則としてコード変更を行わない。

### 3.3 実装
- `app/` 変更時:
  - 対象ワークスペースのみを変更する（`core` / `api/*` / `trigger`）。
  - 既存のモジュール構成（`entity` / `usecase` / `infra`）に沿って実装する。
- `infra/` 変更時:
  - Terraform のモジュール境界を維持し、環境差分（`stage` など）を壊さない。
  - IAM やスケジューラ設定の変更は副作用を明示する。
- コメントは「なぜ必要か」が伝わる場合のみ追加する。

### 3.4 検証
- 実装後は最低限以下を実施する。
  - `app/`: 変更対象パッケージのテスト実行
  - 必要に応じて `npm run build`（`app/`）
  - `infra/`: `terraform fmt -check` と `terraform validate`（対象ディレクトリ）
- 検証できなかった項目は、理由と未確認リスクを明記する。

### 3.5 報告
- 最終報告には以下を含める。
  - 変更したファイル
  - 変更内容の要点
  - 実施した検証コマンドと結果
  - 未実施検証と理由（あれば）

## 4. プロジェクト固有の実行コマンド
- ルート: `cd app`
- ビルド: `npm run build`
- 例: `core` テスト: `npm run test -w @shared/core`
- 例: `trigger` テスト: `npm run test -w @function/trigger`
- 例: API 関連テスト: `npm run test -w @function/create -w @function/update -w @function/delete`

## 5. セキュリティと運用上の注意
- シークレットや資格情報をコード・ログ・ドキュメントに出力しない。
- AWS リソース名、ARN、環境変数の変更は、影響する Terraform と Lambda 実装をセットで確認する。
- 監視・通知・スケジューラ周りの変更は、失敗時挙動（再試行、アラート有無）を確認する。
